# Goal: target end-effector pose (frame_id must be set)
geometry_msgs/PoseStamped target_pose
# If true, only plan (no execution); use false to execute after a successful plan.
bool plan_only
---
# Result
bool success
string message
---
# Feedback (0.0 to 1.0, optional)
float32 progress
